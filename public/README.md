# public 자산 안내

아래 파일을 이 폴더에 넣으면 사이트에 자동으로 반영됩니다.

- `portfolio.pdf` — Download Portfolio 버튼이 내려받는 실제 포트폴리오 PDF
- `og.png` — SNS 공유 시 노출되는 Open Graph 이미지 (권장 1200×630)

파일 경로는 `src/data/profile.ts`(portfolioPdf)와 `src/app/layout.tsx`(og.png)에서
수정할 수 있습니다.
